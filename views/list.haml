%form{ :action => "/save", :method => "post"}
	%select{ :multiple => "true", :name => "files[]"}
		- @files.each do |file|
			%option{:value => file.downcase} #{file.partition('/').last}
	%input{:type => "submit", "value" => "send"}
